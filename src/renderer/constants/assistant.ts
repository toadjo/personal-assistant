/** Shown when the renderer runs without the Electron preload bridge (e.g. plain browser). */
export const PRELOAD_BRIDGE_MISSING_MESSAGE =
  "This window is not running inside the Electron app (preload bridge missing). Open the app from the tray or use npm run dev.";
